# Kotlin configuration

Centralized Gradle properties and ktlint rules for kotlin/ modules. Source of truth: AGENTS.md version pins (Kotlin 2.0.21, Gradle 8.10.2, ktlint 12.1.1, JDK 17, Android SDK 35). Note: gradle.properties is consumed by copy into kotlin/ — see docs/architecture/KOTLIN_CONFIG_INTEGRATION.md (pending M5).
