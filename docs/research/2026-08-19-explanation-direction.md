# Explanation direction

User-facing explanations should be generated from typed plan/task/policy/evidence state and cite the underlying records in-product. LLM summarization can translate those facts into clearer language but must not invent completion, approval, or verification state.

For major architecture decisions, expose the chosen option, key criteria/evidence, and reversible consequences without exposing hidden chain-of-thought.
