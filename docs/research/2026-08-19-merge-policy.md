# Merge policy for production evolution slices

Do not merge merely because implementation is plausible. Require relevant CI/evaluation evidence and review resolution. Merge remains an explicit repository side effect; the development loop prepares a reviewed branch/PR and surfaces readiness rather than silently merging under generic autonomy.
