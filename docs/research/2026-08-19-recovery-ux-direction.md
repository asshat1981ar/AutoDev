# Recovery UX direction

Recovery UI should state what is known versus uncertain. Example structure: last safe checkpoint, interrupted operation, observed current state, AutoDev's reconciliation conclusion, and next safe action.

Do not present a generic retry button when retry could duplicate an uncertain effect. When automatic reconciliation is impossible, request the smallest human confirmation/decision with supporting evidence.
