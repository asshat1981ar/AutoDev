# Agent selection direction

Agent selection should match task requirements to role/capability/model/profile evidence rather than defaulting every task to a generalist. Specialist delegation is justified when it reduces context interference, improves verification outcomes, or isolates risky tool surfaces.

The selector should retain fallback behavior when no specialist meets requirements. Historical success influences ranking but does not override policy or task-specific capability constraints.
