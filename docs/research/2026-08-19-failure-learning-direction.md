# Failure-learning direction

When a run fails, separate task-specific failure from reusable harness weakness. Reusable findings should become one or more of: regression test, failure-memory document, policy rule, verifier improvement, context rule, diagnostic, or evaluation fixture.

Learning from failure does not mean automatically rewriting production policy. Proposed harness/process changes pass the same evaluation and review gates as other changes.
