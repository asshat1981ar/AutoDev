# Review philosophy

Per-slice review checks spec compliance and code quality; final review checks cross-file architecture and regressions. Findings are resolved with code/tests or explicitly recorded rulings—never silently discarded.

Reviewers should focus on observable defects and trust-boundary violations rather than style preferences already enforced by formatters. Verification evidence is not replaced by reviewer confidence.
