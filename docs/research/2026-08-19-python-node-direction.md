# Python/Node fabric direction

Python/Cline and Node/Termux tooling should remain integration/bootstrap/developer-experience surfaces rather than alternate trusted execution kernels. Keep dependency footprint low where repository rules require it and preserve Android portability.

Harness import/export, diagnostics, and compatibility tooling may live here when appropriate, but effectful execution still routes through typed ForgeCore/control-plane boundaries.
