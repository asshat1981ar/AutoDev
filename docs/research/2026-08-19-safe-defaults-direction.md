# Safe defaults direction

Default profiles should minimize effect scope, require independent verification, use finite autonomy budgets, and prefer reversible/checkpointed changes. More permissive configurations require explicit policy/approval rather than hidden convenience flags.

Defaults should be evaluated on representative tasks and Android constraints. A default that only works in a desktop/container environment is not an Android-first default.
