# Approval UX direction

An approval request should identify the exact pending effect, target, requested capability, risk classification, originating task/agent/profile, relevant diff/preview, and whether approval is one-shot or otherwise scoped by policy.

The UI must never reduce approval to an unexplained generic "allow agent" control. If the planned effect changes after review, stale approval should not silently authorize the changed operation.
