# Remote companion direction

A remote companion may provide heavier models, build infrastructure, long-lived processes, containers, or platform services unavailable on Android. It should expose the same typed plan/task/evidence contracts and remain subordinate to ForgeCore authorization semantics.

Android can therefore be the primary control surface without pretending the phone must host every compiler/model/runtime. Remote execution must remain observable, capability-scoped, and recoverable across disconnects.
