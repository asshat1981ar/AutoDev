# CI failure memory rule

If CI reveals a reusable harness/process failure rather than a one-off code typo, add or update `docs/failures/` with root cause, prevention, and enforceable evidence before closing Milestone 1. Do not create failure-memory noise for ordinary compiler diagnostics already prevented by the existing gate.
