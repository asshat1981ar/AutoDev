# Run completion direction

A run is complete only when all required milestones/tasks are in terminal successful states and every declared required verification check is present and passing. The completion record should reference final repository/artifact state and unresolved limitations.

A model/agent saying "done", a clean patch application, or passing only the checks that happened to run is insufficient. This remains consistent with AutoDev's existing evidence contract.
