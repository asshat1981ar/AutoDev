# Prompt asset direction

Prompt assets should be versioned, provenance-tracked instructional components with explicit intended scope (system prefix/suffix, role instructions, task template, verifier explanation, etc.). Prompt composition order must be deterministic and inspectable.

Prompts can influence agent behavior but cannot override typed policy, authorization, or evidence requirements. Evaluation should record prompt/profile versions so behavior changes are attributable.
