# Mobile command/intent direction

A compact command surface can let users start objectives, reprioritize, cancel, resume, request explanation, inspect evidence, or switch harness profiles without navigating deep menus. Commands should resolve to typed control-plane intent and preview consequential effects.

Natural-language commands remain suggestions to the planner/control plane; they do not bypass typed approval or policy flows.
