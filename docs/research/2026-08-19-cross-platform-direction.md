# Multiplatform direction

Android is the reference interactive client, while portable contracts should live in KMP/server/Rust layers so desktop/web companions render the same durable run semantics.

Platform clients may differ in layout, local integrations, and background capabilities. They must not diverge on plan/task/envelope identities, approval scope, evidence meaning, or lifecycle transitions. Cross-platform parity is semantic parity, not pixel parity.
