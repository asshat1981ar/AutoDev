# CI trigger note

The focused workflow triggers on pushes to `feat/execplan-control-plane` and pull requests targeting `main`. PR creation therefore provides a clean point to inspect both focused and repository-level checks against the final branch head.
