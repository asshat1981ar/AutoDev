# Agent communication direction

Prefer communication through typed task inputs, artifact/evidence references, and structured outputs over unrestricted agent-to-agent chat. This reduces context growth and makes delegation reproducible/auditable.

When negotiation/critique between agents is useful, persist the resulting decision/findings rather than the entire dialogue unless the dialogue itself is required evidence.
