# Configuration UX direction

Prefer opinionated safe defaults with inspectable profiles over requiring users to assemble dozens of agent/tool settings. Advanced configuration can expose profiles/assets and policy constraints progressively.

When configuration is invalid or incompatible, explain the exact asset/dependency/platform/policy conflict and propose safe alternatives. Never resolve a conflict by silently dropping a security requirement.
