# Human intervention direction

Human intervention should be modeled as typed causes rather than generic pauses: approval required, ambiguous reconciliation, product decision, unavailable credential/resource, conflicting external change, or exhausted automation budget.

The system should present the smallest decision necessary and preserve the run around it. Resuming after intervention must validate that the answer still applies to the current effect/state rather than treating any human response as a universal override.
