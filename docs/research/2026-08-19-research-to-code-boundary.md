# Research-to-code boundary

The `docs/research/` material is a hypothesis/decision backlog, not implementation. Code enters production only through an active milestone plan with tests and review.

This separation lets AutoDev explore aggressively without silently expanding the runtime surface. Research findings may amend specs/ADRs when they become binding; otherwise they remain advisory inputs to future planning.
