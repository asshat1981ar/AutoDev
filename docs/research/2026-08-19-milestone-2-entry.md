# Milestone 2 entry criteria — Harness Asset Protocol

Begin production implementation only after Milestone 1 exit criteria are met.

Required preparation:

- refreshed Harness Protocol v1 primary-source review;
- completed schema-gap matrix;
- current AutoDev plugin/skill/MCP/policy model inspection;
- refreshed Deep Agents profile/subagent research for profile semantics;
- ADR selecting internal representation and external compatibility strategy;
- adversarial test plan proving imported permissions/config cannot create trusted authority.
