# Planner direction

The planner transforms an objective plus repository/context evidence into milestones/tasks, acceptance criteria, dependencies, capability/risk expectations, and verifier requirements. It does not authorize those capabilities.

Planning should explicitly mark uncertainty and experiments. High-uncertainty architecture choices may begin with proof-of-concept/evaluation milestones before committing to a large implementation path.
