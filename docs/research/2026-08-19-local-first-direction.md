# Local-first direction

AutoDev should remain useful with local repository access and locally available models/tools while allowing remote companions/providers when configured. Durable coordination and core evidence semantics should not depend on a single cloud service.

Feature capability may degrade offline or on Android, but the system should explain that degradation explicitly and preserve queued intent/recovery state rather than failing opaquely.
