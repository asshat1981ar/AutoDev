# Debugging direction

Debug views should correlate run timeline, resolved harness configuration, context selections, policy decisions, effects, verifier evidence, and checkpoints by stable ids. This is more useful than a single interleaved text log.

Users get concise diagnostics; developers can expand into provenance and raw logs/artifacts where policy permits. Debug tooling remains observational and cannot mutate trusted state implicitly.
