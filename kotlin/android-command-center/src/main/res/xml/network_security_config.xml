<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext is permitted ONLY to the loopback address the Android emulator
  uses to reach the host machine (`10.0.2.2`) and to localhost for on-device
  smoke tests. Production endpoints must use HTTPS; do NOT broaden this
  allow-list without an explicit security review.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
