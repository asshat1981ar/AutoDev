# AMCX-1 Schemas

Draft-07 JSON Schemas for AMCX-1 v1.1 artifacts. Validated structurally by `scripts/check_harness_drift.py --amcx1` (stdlib-only: required fields, digest format, duplicate logical_identity).
