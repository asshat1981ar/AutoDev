name: One-shot evaluator lockfile finalizer

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  finalize:
    if: github.head_ref == 'feat/self-evaluation-factory-v0' && github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
      - uses: dtolnay/rust-toolchain@stable
      - name: Generate and push canonical workspace lockfile only
        run: |
          cd crates
          cargo generate-lockfile
          cd ..
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add crates/Cargo.lock
          if git diff --cached --quiet; then
            echo "Cargo.lock already canonical"
            exit 0
          fi
          git commit -m "chore(eval): regenerate workspace lockfile"
          git push origin HEAD:${{ github.head_ref }}
